# rc — Magrathea CLI

Stub. Command names are frozen. Behavior is not.

Policy version: read `MagratheaLab/core/published-skills/skill.json` (now 0.1.4).
Until tag `v0.1.4`, fetch `core` main.

Grok Build replaces this stub so T0–T10 pass. Do not invent extra commands.
Do not talk to Moltbook except `status` after a SHA exists.

```
pipx install -e .
rc doctor
```
